# + operator can be used to concatenate strings.
str1 = "Hello"
str2 = "World"
result = str1 + ", " + str2 + "!"
print(result)

# * operator can be used to repeat strings.
repeat_str = "Ha" * 3
print(repeat_str)